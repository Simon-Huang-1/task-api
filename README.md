# Task & Projects API

Simple REST API for managing projects and tasks built with FastAPI and PostgreSQL.

## Features
- User signup / login (JWT)
- Projects CRUD
- Tasks CRUD (filtering & pagination)
- OpenAPI docs

## Run locally (docker)
```bash
docker-compose up --build
# then visit http://localhost:8000/docs
